Remove-Item -Recurse -Force -Path "node_modules" 
Remove-Item -Force -Path "package-lock.json" 
Remove-Item -Force -Path "yarn.lock" 
yarn install
npx react-native autolink
npx react-native codegen --path ./node_modules/react-native
npx expo prebuild
npx react-native-asset
cd android
./gradlew assembleRelease
npx expo run:android
